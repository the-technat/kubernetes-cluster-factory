apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: akeyless-tailscale-oauth-client
  annotations:
    argocd.argoproj.io/hook: PreSync
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: akeyless-secret-store
  target:
    name: operator-oauth
    creationPolicy: Owner
  data:
    - secretKey: client_secret
      remoteRef:
        key: /alleaffengaffen/l-t/tailscale/tailscale-oauth-client-secret
    - secretKey: client_id
      remoteRef:
        key: /alleaffengaffen/l-t/tailscale/tailscale-oauth-client-id
